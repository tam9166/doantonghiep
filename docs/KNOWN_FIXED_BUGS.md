# Known bug audit

Statuses: **FIXED** means source/test evidence is present; **STILL EXISTS** means source evidence confirms it; **NOT VERIFIED** needs an executable environment or deeper scenario testing; **NOT APPLICABLE** means the feature is absent.

| ID | Severity | Flow | Status | Evidence / next action |
|---|---|---|---|---|
| BUG-001 | P0 | External API | FIXED | `src/services/api.js` has separate `externalApi`; `api.test.js` checks no auth/captcha headers. |
| BUG-002 | P0 | Timekeeping | FIXED | `SelfServiceDataIsolationIntegrationTest` proves check-in and personal attendance ignore a forged username and use the authenticated principal. |
| BUG-003 | P0 | Staff | FIXED | Two-user integration coverage proves personal schedules and service zones cannot be read via a forged username; V044 supplies the required service-zone table. |
| BUG-004 | P0 | Account API | FIXED | Sensitive `Account` fields are ignored/write-only and regression coverage audits direct serialization plus staff, customer and order responses. |
| BUG-005 | P0 | Order | FIXED | A database integration test forces a failure at the final activity-log step and proves details, totals, inventory and idempotency data all roll back. |
| BUG-006 | P0 | Inventory | FIXED | Database-backed shortage coverage proves no detail/operation is created and order totals plus batch/ingredient stock remain unchanged. |
| BUG-007 | P0 | Inventory | FIXED | Concurrent database requests for the last unit yield exactly one success and one shortage conflict; pessimistic locks prevent negative stock. |
| BUG-008 | P0 | Order | FIXED | Repeating the same add-items request and idempotency key returns the stored result, creates one detail/operation and consumes inventory once. |
| BUG-009 | P0 | Money | FIXED | Money, inventory quantities, recipe amounts and tax rates use `BigDecimal`; V045 converts the six remaining live `FLOAT` columns to exact `DECIMAL` types. |
| BUG-010 | P0 | JWT | FIXED | Production and staging profiles fail fast without a configured JWT secret; `ApplicationStartupValidatorTest` covers both profile families. |
| BUG-011 | P0 | Credentials | FIXED | Production/staging startup rejects plaintext hashes and common weak/demo passwords for every account; tracked deployment configuration contains placeholders only. |
| BUG-012 | P1 | Password route | FIXED | `/change-password` is routed by frontend and SPA fallback. |
| BUG-013 | P1 | Authorization | FIXED | Frontend admin-route matrix now matches backend operational roles; frontend and backend matrix regressions pass. |
| BUG-014 | P1 | Error handling | FIXED | Global/security/filter errors use the stable JSON contract with explicit `application/json` and correlation IDs. |
| BUG-015 | P1 | Rate limit | FIXED | Policies match exact method/path; public chatbot's tighter rule is ordered before the broad AI matcher and covered by regression. |
| BUG-016 | P1 | Rate limit | FIXED | Client identity uses the servlet remote address and ignores attacker-controlled forwarding headers; bypass regression passes. |
| BUG-017 | P1 | Rate limit | FIXED | Production uses the bounded/expiring SQL Server store from V047; two independent service instances share one quota in the database regression. |
| BUG-018 | P1 | Review | FIXED | Public projection excludes reservation ID/code, phone and moderation fields; admin projection remains role-protected. |
| BUG-019 | P1 | Reservation lookup | FIXED | Public lookup is POST body with code + phone. |
| BUG-020 | P1 | Review | FIXED | Null/out-of-range rating returns 422 in controller. |
| BUG-021 | P1 | Review | FIXED | Ratings, phone, URL and text limits/formats are validated; V046 enforces one review per reservation under concurrency. |
| BUG-022 | P1 | Booking | FIXED | Booking codes use a date plus random UUID suffix, are checked before persistence, returned from the saved entity, and V048 enforces database uniqueness. |
| BUG-023 | P1 | Booking | FIXED | Legacy guest-booking is gone; dine-in data uses normalized `order_type`/`table_id` and no longer stores table metadata in address. |
| BUG-024 | P1 | Reservation table | FIXED | Checkout, open-order lookup, table status, refund, merge and split use exact `tableId`/FK paths with no address substring inference. |
| BUG-025 | P1 | Migration | FIXED | V001/V002 provide the official baseline; the blank-database regression created a temporary SQL Server database and applied all 50 versioned migrations successfully. |
| BUG-026 | P1 | Payment config | FIXED | Demo values are dev-only; production requires external payment values, disables demo mode and fails startup for unsafe configuration. |
| BUG-027 | P1 | Recruitment upload | FIXED | Private UUID storage validates extension/MIME/magic and DOCX structure, blocks active content/traversal/ZIP bombs, restricts download, and purges expired CVs. |
| BUG-028 | P2 | Timekeeping | NOT VERIFIED | Check-out preservation needs scenario test. |
| BUG-029 | P2 | Date parsing | NOT VERIFIED | Partial Java-time conversion observed; complete audit pending. |
| BUG-030 | P2 | Timezone | NOT VERIFIED | Vietnam zone exists in timekeeping; cross-flow verification pending. |
| BUG-031 | P2 | Reservation | FIXED | `RestaurantBusinessHoursService` centralizes configured hours. |
| BUG-032 | P2 | Persistence | NOT VERIFIED | Query profiling pending. |
| BUG-033 | P2 | Lists | NOT VERIFIED | Admin endpoint pagination audit pending. |
| BUG-034 | P2 | Auth | FIXED | Active customer/staff identities and the selected auth context are stored in window-scoped `sessionStorage`; multi-context regressions prove login/logout/401 handling does not cross windows. |
| BUG-035 | P2 | CSRF | NOT VERIFIED | Verify cookie/token architecture and endpoints. |
| BUG-036 | P2 | Roles | NOT VERIFIED | Route/controller permission duplication audit pending. |
| BUG-037 | P2 | SPA | FIXED | Explicit SPA routes include change-password and event booking; API/assets excluded. |
| BUG-038 | P2 | Spring config | STILL EXISTS | Deprecated `WebMvcConfigurer` converter/path APIs emit compiler warnings. |
| BUG-039 | P2 | CI | NOT VERIFIED | Linux execute bit must be checked in a Linux checkout. |
| BUG-040 | P2 | Repository hygiene | STILL EXISTS | Runtime artifacts/logs/backups/zip are present locally; do not delete without owner approval. |
| BUG-041 | P2 | Documentation | NOT VERIFIED | README and deployment docs require source comparison. |
| BUG-042 | P2 | Frontend | NOT VERIFIED | Large components exist; no safe refactor selected. |
| BUG-043 | P2 | Frontend tests | STILL EXISTS | Only limited unit tests were found; broaden coverage. |
| BUG-044 | P2 | UX | STILL EXISTS | Many production views call native alert/confirm/prompt. |
| BUG-045 | P2 | Frontend API | STILL EXISTS | Many views call authenticated Axios directly instead of shared service layer. |
| BUG-046 | P2 | JPA | NOT VERIFIED | `@Data` appears in entities; relation/logging audit pending. |
| BUG-047 | P2 | Account roles | FIXED | Authentication repository query fetches authorities and roles explicitly while the general entity relation remains LAZY. |
| BUG-048 | P2 | Errors | NOT VERIFIED | Raw exception exposure audit pending. |
| BUG-049 | P2 | PII | NOT VERIFIED | Retention/access policy not documented. |
| BUG-050 | P2 | Performance | NOT VERIFIED | Build code-splits views; dashboard/query measurement pending. |

## New defects fixed this audit

| ID | Severity | Root cause | Fix | Regression |
|---|---|---|---|---|
| BUG-051 | P0 | Uncommitted edits removed imports/methods and used invalid Java identifiers, leaving backend uncompilable. | Restored compatible imports, service wiring, method signatures and repository contract. | `mvnw.cmd -DskipTests compile` passes. |
| BUG-052 | P1 | AI availability queried every table instead of the repository contract for active, ordered tables. | Use `findByActiveTrueOrderByAreaIdAscIdAsc()`. | FIXED: `AiAvailabilityToolServiceTest`; full backend suite passed. |
| BUG-053 | P2 | Last-order comparison included the exact cutoff despite the documented “before cutoff” rule and did not handle overnight hours. | Require a strict cutoff and handle same-day/overnight windows explicitly. | FIXED: `RestaurantBusinessHoursServiceTest`; full backend suite passed. |
| BUG-054 | P1 | Conversation date parsing accepted an injected clock but called `LocalDate.now(zoneId)`. | Derive today with `LocalDate.ofInstant(now, zoneId)`. | FIXED: fixed-clock regressions; full backend suite passed. |
| BUG-055 | P2 | Expiry intent used broad “sắp hết” keywords and unordered intent iteration, so low stock could be misclassified as expiration. | Reserve expiry intent for explicit expiry phrases and define deterministic intent precedence. | FIXED: `RoleAwareAssistantServiceTest`; full backend suite passed. |
| BUG-056 | P1 | The guest-count ternary mixed primitive parsing with a nullable fallback, causing Java to unbox `null` when a message contained no guest count. | Box the parsed branch explicitly so a nullable fallback remains valid. | FIXED: `AiConversationMemoryServiceTest`; full backend suite passed. |
| BUG-057 | P0 | Staff login loaded a LAZY authority collection after the repository session closed, so valid credentials returned HTTP 500. | Add a dedicated authentication query with an entity graph for authorities and roles. | FIXED: `AccountTokenSecurityTest` plus real `admin` login against SQL Server. |
| BUG-058 | P0 | Staff, popular-item and reservation admin screens queried LAZY data incorrectly; the reservation graph also named unmapped attributes. | Add scoped fetch queries for staff/order details and correct the reservation entity graph. | FIXED: `AdminDataVisibilityRegressionTest` plus authenticated API smoke checks. |
| BUG-059 | P1 | The in-memory rate limiter was process-local and had no production-wide quota or durable cleanup bound. | Add a transactional SQL Server quota store with expiry cleanup and a 100,000-row cap while retaining the bounded Caffeine store for local use. | FIXED: `RateLimitDatabaseIntegrationTest`, `RateLimitServiceTest`, and `RateLimitingFilterTest`. |
| BUG-060 | P1 | Sequential booking codes could race across requests and schema uniqueness was not guaranteed by an official migration. | Generate high-entropy date-prefixed codes, verify nonexistence, return the saved value, and add V048's unique index. | FIXED: 100-code uniqueness regression and successful V048 migration. |
| BUG-061 | P1 | Flyway began at V003, so a new database depended on destructive manual setup scripts and also lacked `reservation_waitlist`. | Add non-destructive V001 schema and V002 seed baselines including the missing waitlist schema. | FIXED: `BlankDatabaseMigrationIntegrationTest` applies V001-V048 to a real temporary SQL Server database and verifies core tables/data. |
| BUG-062 | P1 | Recruitment files trusted shallow metadata, lived under a weak path contract, and had no retention lifecycle. | Introduce private safe-name storage, layered content/ZIP validation, attachment-only authorized downloads, generic errors, and scheduled retention. | FIXED: `CvFileStorageServiceTest` and `CvRetentionServiceTest`. |
| BUG-063 | P1 | The Security and MVC SPA route lists diverged, so direct refresh of event booking/change-password returned 401 and AI knowledge returned 500. | Use one `SpaRouteRegistry` for authorization and forwarding while keeping static files out of the forward registry. | FIXED: `SpaRouteRegistryTest` plus runtime smoke of all 34 Vue routes. |
| BUG-064 | P1 | SQL Server bound the no-show `LocalTime` parameter as `DATETIME`, which could not be compared with the `arrival_time TIME` column and crashed every expiry scan. | Explicitly cast the HQL parameter to `LocalTime` so SQL Server receives a compatible `TIME` comparison. | FIXED: `ReservationExpiryQueryIntegrationTest` plus repeated five-second runtime expiry scans. |
| BUG-065 | P1 | The hot-menu native query and `OrderDetail` mapping used `OrderDetails`, while the canonical Flyway schema uses `order_details`; `/api/menu/hot` failed at runtime. | Align the entity mapping and native query with the canonical `order_details` table. | FIXED: `HotMenuItemQueryIntegrationTest` plus packaged-browser HTTP smoke. |
| BUG-066 | P1 | The public dine-in page requested the admin-only popular-items endpoint, so guest browsers received 401 and lost the frequently ordered suggestions. | Load suggestions from the public `/api/menu/hot` endpoint without an admin bearer dependency. | FIXED: `publicEndpointContracts.test.js` plus packaged-browser HTTP smoke. |

## Master Prompt V2 continuation audit (2026-08-20)

These labels are prefixed with `V2-` because the supplied prompt reused IDs already present above.

| ID | Severity | Status | Evidence / disposition |
|---|---|---|---|
| V2-BUG-051 | P1 | FIXED | V049 repaired 149 existing table-linked orders from `TAKEAWAY` to `DINE_IN`, removed the database default, and checkout now rejects a missing `orderType` with 422. |
| V2-BUG-052 | P1 | FIXED | Base/prod/production rate limiting defaults to enabled; only explicit dev/test/local-example profiles default to disabled. |
| V2-BUG-053 | P1 | FIXED | The scheduled reservation expiry job now queries at most 200 matching candidate IDs, fetches only that batch, and V050 adds the supporting indexes. |
| V2-BUG-054 | P1 | FIXED | Customer (`token`/`user`) and staff (`staff_token`/`staff_user`) namespaces have separate readers/logout operations; staff screens no longer fall back to customer credentials. |
| V2-BUG-055 | P2 | INCOMPLETE_FEATURE | `contact_task_status` exists only in V041 and the entity mapping. The implemented contact workflow uses `contact_status`, call note, actor, and timestamp fields from V036. Do not invent a second workflow until product requirements define its states, transitions, ownership, and UI. |
| V2-BUG-056 | P1 | FIXED | A Flyway `beforeMigrate` callback gives actionable duplicate-data failures before V046/V048; integration tests cover blank, valid legacy, duplicate review, and duplicate booking-code databases. |
| V2-BUG-057 | P1 | FIXED | Kitchen status/toggle/group/recipe/AI failures use the shared safe backend error extractor with user-safe fallbacks. |
| V2-BUG-058 | P1 | FIXED | Remaining legacy green/gold/cream literals were moved to canonical burgundy/navy and semantic tokens; a source regression prevents the legacy palette from returning. |

## Cancellation and inventory audit (2026-08-21)

| ID | Severity | Root cause | Fix | Regression |
|---|---|---|---|---|
| BUG-067 | P0 | Reservation refunds were marked completed without evidence of an external refund. | Keep refunds pending until a unique provider reference is supplied and a successful REFUND ledger transaction is persisted. | `RefundServiceTest`, `ReservationCancellationServiceTest` |
| BUG-068 | P0 | Customers had no safe cancellation-request flow and refund eligibility did not follow the 12-hour/50%-of-paid-deposit policy. | Add V054, same-booking 2/4 verification, policy snapshots, admin approval queue and exact Vietnam-time calculation. | `ReservationCancellationPolicyTest`, `ReservationCancellationServiceTest` |
| BUG-069 | P1 | Public cancellation creation could return the verified reservation's full PII. | Return a minimal receipt containing only request code, status and generic message. | Compile-time public receipt contract plus cancellation service regressions. |
| BUG-070 | P1 | Contact updates overwrote the only evidence of prior calls. | Append `reservation_contact_logs` on every update while retaining current summary fields. | `ReservationServiceTest` |
| BUG-071 | P0 | Menu availability was boolean/aggregate-based and carts could exceed recipe-backed batch stock. | Compute a numeric limiting serving count from active batches, expose it publicly and cap both customer carts; backend rejects bypass attempts. | `MenuAvailabilityServiceTest`, `OrderCheckoutServiceTest`, `publicEndpointContracts.test.js` |
| BUG-072 | P1 | The ingredient API could mutate aggregate quantity without changing FIFO batches. | Make batch sums authoritative and reject direct aggregate quantity writes. | Source contract plus menu availability tests. |
| BUG-073 | P0 | Checkout permanently deducted FIFO batches before an order was paid or accepted, so abandoned/expired orders destroyed sellable stock. | Add V055 per-order ingredient holds; availability subtracts active holds, payment/manual dispatch consumes once, cancellation releases, expired payment is manual-review and releases the hold. | `InventoryReservationServiceTest`, `MenuAvailabilityServiceTest`, `OrderPaymentServiceTest`, `PaymentLedgerServiceTest`, blank/legacy migration tests. |
| BUG-074 | P0 | Kitchen cancellation only changed the detail status, leaving order totals, paid balance, refund state, stock and actor audit inconsistent. | Lock detail/order, adjust recipe inventory according to hold/consumed/start state, recalculate money, create only a pending excess-payment refund, record actor/reason/time and cancel an empty parent order. | `KitchenOrderDetailServiceTest`, `InventoryReservationServiceTest`, V056 blank/legacy migration tests. |
| BUG-075 | P0 | Controllers and payment/kitchen/refund services wrote undocumented numeric order states directly, allowing skipped, unknown and rollback transitions. | Add the canonical `OrderStatus` enum and route every operational order-status write through `OrderStateMachineService`. | `OrderStateMachineServiceTest`, `OrderWorkflowGuardTest`, full backend suite. |
| BUG-076 | P1 | The method policy described Waiter serving, but the HTTP security matcher excluded WAITER and the generic mutation endpoint still owned dish workflow logic. | Permit WAITER on the matcher while retaining method-level `status == 2`, and delegate serving to the locked kitchen-detail service. | `EndpointAuthorizationMatrixTest`, `KitchenOrderDetailServiceTest`. |
| BUG-077 | P0 | Merge/split moved details while ignoring paid amounts, intents, ledger/refunds, vouchers and inventory holds; split also silently skipped invalid detail IDs. | Lock involved orders, safely block unsupported financial/stock allocation, validate the complete detail selection, and recalculate unpaid totals/remaining balances atomically. | `OrderFinancialMutationGuardServiceTest`, `OrderWorkflowGuardTest`. |
| BUG-078 | P1 | Table transfer could move an order onto an occupied table and leave both source/target QR capabilities valid. | Lock and validate target occupancy/active-order state, release source conditionally, occupy target and revoke both table sessions transactionally. | `OrderWorkflowGuardTest`. |
| BUG-079 | P0 | Table status and unlink endpoints had divergent release checks and could bypass pending-payment, refund and unserved-dish safeguards. | Route both through `TableReleaseGuardService`; only a fully paid, fully served order is completed/released. | `TableReleaseGuardServiceTest`, `OrderWorkflowGuardTest`. |
| BUG-080 | P0 | Event booking promised a deposit but did not persist an owner/expiry, issue a payment capability, expose an immediate QR path, notify staff/realtime clients, or receive dedicated public abuse controls. | Mirror the reservation payment-capability lifecycle, auto-create QR in the event UI, and add event-specific CAPTCHA/rate policies. | `ReservationServiceTest`, `CaptchaFilterTest`, `RateLimitingFilterTest`, `captcha.test.js`. |
| BUG-081 | P1 | Registration displayed email as optional and only checked password length in the browser although the backend/DB require email and reject common passwords. | Make email visibly required, normalize the submitted identity fields, and share the backend username/email/password constraints in pre-confirmation and pre-submit frontend validation. | `AuthRequestValidationTest`, `PasswordPolicyTest`, `registrationValidation.test.js`. |
| BUG-082 | P1 | The Kitchen “Cá nhân” action opened the customer `/profile`, while staff-profile routing was implicit and Kitchen logout left the active auth context behind. | Add the dedicated `/staff/profile` route backed by the existing staff schedule/timekeeping profile, point every staff role to it, classify it as staff auth context, and clear logout through the shared session service. | `index.test.js`, `session.test.js`, `api.test.js`, `SpaRouteRegistryTest`. |
| BUG-083 | P1 | The waitlist submit flow and no-table information existed in `Reservation.vue` but no live UI action could reach it; obsolete manual table-map code remained after switching to backend auto-assignment. | Show single/combined availability at the auto-assignment step, offer a real waitlist action only when neither is available, retain large-group event routing, and remove the disconnected manual-selection functions. | `publicEndpointContracts.test.js`, `ReservationWaitlistServiceTest`, `RateLimitingFilterTest`. |
| BUG-084 | P0 | `/api/tables/check-availability` ignored its date/time and returned every table; overlap checks also missed a prior-date booking continuing past midnight. | Delegate both availability routes to the canonical reservation service with date/time/duration/guest/area/late-dining inputs and compare request/reservation windows as `LocalDateTime`, including the previous date. | `RestaurantTableAvailabilityControllerTest`, `ReservationServiceTest`, `RestaurantBusinessHoursServiceTest`. |
| BUG-085 | P1 | Delivery checkout concatenated recipient name, phone and address into one legacy `address` string, and direct API callers could omit recipient identity. | Add conditionally required structured delivery fields, server-side phone validation, separate persistence/response fields and an optional delivery note. | `OrderCheckoutServiceTest`, `orderCheckout.test.js`. |
| BUG-086 | P1 | Voucher reporting multiplied an already-discounted final total by the combined membership-plus-voucher rate, overstating/understating the actual voucher benefit. | Calculate membership then voucher reductions explicitly, persist each money component and record only the voucher amount against its pre-voucher base. | `OrderCheckoutServiceTest`. |
| BUG-087 | P0 | `work_schedules.shift_name/start_time/end_time` were NOT NULL in SQL Server but unmapped and unwritten by JPA, causing schedule inserts to fail. | Map the complete snapshot, derive it from one canonical shift definition and add V057 repair/backfill migration. | `WorkShiftDefinitionTest`, `SelfServiceDataIsolationIntegrationTest`, blank/legacy migration tests. |
| BUG-088 | P0 | Timekeeping had no database uniqueness, check-in was race-prone, lateness used hard-coded 06:15/14:15/22:15 clocks, and check-out did not persist `total_hours`. | Lock the employee identity transactionally, enforce `(username, work_date)` in V058, derive lateness from the assigned schedule and calculate overnight-safe hours once on check-out. | `AttendancePolicyServiceTest`, `SelfServiceDataIsolationIntegrationTest`, blank/legacy migration tests. |
| BUG-089 | P1 | Kitchen polled the complete admin order graph every five seconds and also reloaded ingredients/products on every tick despite an existing WebSocket feed. | Add a status/date-scoped kitchen-board query, retain WebSocket as primary and use a 60-second tab-aware fallback. | `OperationalOrderQueryScopeTest`, `kitchenPerformance.test.js`, `EndpointAuthorizationMatrixTest`. |
| BUG-090 | P1 | Operational screens and reports loaded complete order/table/recipe datasets and filtered them in memory, causing work to grow with all historical data. | Add bounded/status/date repository queries for admin, dashboard, revenue, waiter, kitchen, reservation, popular-items, purchase-suggestion and staff-assistant flows. | `OperationalOrderQueryScopeTest`, `AdminDataVisibilityRegressionTest`, `ReservationServiceTest`, `RoleAwareAssistantServiceTest`. |
| BUG-091 | P1 | Popular-ingredient sorting cast a persisted `BigDecimal` consumption value to primitive `double`, throwing `ClassCastException` as soon as the report contained data. | Compare the `BigDecimal` values directly. | `AdminDataVisibilityRegressionTest`. |
| BUG-092 | P2 | Dangerous refund, cancellation, table and kitchen actions still depended on blocking browser `confirm/prompt`, while outcomes used scattered alerts. | Add one global confirm/prompt dialog, reuse the global toast and migrate the dangerous flows first. | `dialogAndPrint.test.js`, lint and frontend suite. |
| BUG-093 | P1 | Cashier printing replaced `document.body.innerHTML`, printed, restored stale HTML and reloaded the page, destroying live Vue state/listeners. | Clone only the requested invoice/report into an isolated print iframe and clean it up after printing. | `dialogAndPrint.test.js`. |
| BUG-094 | P1 | Multipart applications validated only blank name/phone and accepted malformed email/message/post IDs until persistence. | Bind a validated multipart DTO, enforce field limits/phone/email/post ID and reject a nonexistent recruitment post before file/storage persistence. | `ApplicationRequestValidationTest`, `CvFileStorageServiceTest`. |
| BUG-095 | P2 | Invalid persisted JSON and failures in realtime/AI audit/config/date fallback paths could be silently swallowed. | Fail on corrupt persisted JSON and log intentionally nonfatal fallback failures with context. | `StringListJsonConverterTest` plus focused backend suite. |
| BUG-096 | P1 | A clean lockfile install reported three high-severity transitive dependency advisories. | Upgrade the compatible transitive lockfile versions for `brace-expansion`, `nanoid` and `undici`; clean audit now reports zero vulnerabilities. | `npm ci`, `npm audit`, full frontend verification. |
| BUG-097 | P0 | Cancellation/refund/reservation paths could make a table available independently; order cancellation also released a table while its refund was merely pending. | Introduce `TableLifecycleService` as the only availability writer, guard unpaid/pending-payment/pending-refund/unserved/active-hold invariants, revoke table capabilities transactionally, and retain the table until refund completion. | `TableLifecycleServiceTest`, `TableReleaseGuardServiceTest`, `OrderRefundServiceTest`, `ReservationCancellationServiceTest`, `OrderWorkflowGuardTest`. |
| BUG-098 | P1 | Operational notification list/count/read mutations were global, so one role could enumerate or mark another role's notifications. | Scope every read mutation to `ALL` plus the authenticated authority and restrict alert generation to ADMIN/MANAGER. | `NotificationAuthorizationTest`. |
| BUG-099 | P1 | Retrying an already completed refund returned success before validating the supplied provider reference. | Treat a retry as idempotent only when the ledger reference, aggregate, amount, direction and success status exactly match the completed refund. | `RefundServiceTest`, `OrderRefundServiceTest`. |
| BUG-100 | P1 | Merge/split/transfer locked tables and orders in request-dependent order, leaving races and avoidable deadlock windows. | Acquire affected table/order pessimistic locks in deterministic ID order and reject a transfer if its source changed after the initial snapshot. | `OrderWorkflowGuardTest`; full SQL Server-backed backend suite. |
| BUG-101 | P1 | Reservation voucher consumption used an unlocked read, and waitlist terminal mutations read an unlocked entry. | Lock a voucher before consuming its single use and lock waitlist entries before contact/convert/cancel transitions. | `ReservationVoucherLockingTest`, `ReservationWaitlistServiceTest`. |
| BUG-102 | P1 | Auto-assignment and restaurant-capacity checks compared only `LocalTime` on the requested date, missing a previous-day booking that continued after midnight. | Compare complete `LocalDateTime` windows and include prior-date candidates while retaining transactional capacity/table locks. | `AutoTableAssignmentServiceTest`, `RestaurantCapacityServiceTest`, `ReservationServiceTest`. |
| BUG-103 | P0 | The native/JDBC `sp_getapplock` call ran while SQL Server still reported `@@TRANCOUNT = 0`, so every reservation request carrying an idempotency key received a false 409; competing keys were therefore rejected before capacity serialization. | Execute the application lock on the Spring-managed connection, assert an active Spring transaction, establish the SQL Server transaction when it is still lazy, and retain the transaction-owned exclusive lock until commit/rollback. | `ReservationConcurrencyIntegrationTest`, `ReservationServiceTest`. |
| BUG-104 | P1 | Waitlist conversion accepted an empty or invented reservation code and allowed multiple waitlist entries to claim the same reservation. | Require and pessimistically lock a matching reservation, verify customer/date/guest/area identity, reject an existing claim, and enforce a filtered unique index in V059. | `ReservationWaitlistServiceTest`, `ReservationConcurrencyIntegrationTest`, blank/legacy migration tests. |
| BUG-105 | P2 | Admin reservation/waitlist actions used native prompts; several optional prompts converted browser Cancel into an empty value and continued the mutation. | Move all eight prompts in `AdminReservation.vue` to the shared dialog and distinguish cancellation from an intentionally empty optional note; destructive reasons and waitlist reservation codes are required. | `adminReservationDialog.test.js`, frontend lint/test/build. |
| BUG-106 | P0 | Two callbacks for the same provider transaction could both pass the initial duplicate lookup before either committed; the loser then failed the ledger unique constraint instead of returning an idempotent result. | Acquire a transaction-owned SQL Server application lock keyed by provider transaction ID before the duplicate lookup and retain the ledger uniqueness constraint as defense in depth. | `PaymentConcurrencyIntegrationTest`, `PaymentLedgerServiceTest`, `PaymentWebhookServiceTest`. |
| BUG-107 | P1 | Merging a source table cancelled its order but left the source table's QR capability active. | Revoke every active source-table capability in the same merge transaction after the locked order/table mutation. | `TableWorkflowConcurrencyIntegrationTest`, `OrderWorkflowGuardTest`. |
| BUG-108 | P0 | Table release inspected payment intents without locking them, so a concurrent provider callback could commit after the release decision; duplicate release also repeated lifecycle side effects. | Lock every payment intent for the table before evaluating financial state, treat `OVERPAID` as blocking, and make an already-available release idempotent after rechecking invariants. | `TableReleaseConcurrencyIntegrationTest`, `TableLifecycleServiceTest`, `TableReleaseGuardServiceTest`. |
| BUG-109 | P1 | The waiter UI called the atomic order-transfer endpoint and then independently rewrote both table statuses, so a successful move could be followed by a false error or duplicate lifecycle mutation. | Delegate move state, locking and QR revocation exclusively to the transactional backend endpoint. | `resourceCleanup.test.js`, `OrderWorkflowGuardTest`, `TableWorkflowConcurrencyIntegrationTest`. |
| BUG-110 | P1 | Production profiles inherited the base CAPTCHA default (`false`) and could start with public abuse protection disabled. | Enable a real CAPTCHA provider by default in `prod`/`production` and fail startup when CAPTCHA is disabled, uses `mock`, or lacks its secret. | `ApplicationStartupValidatorTest`, `CorsProfileConfigurationTest`. |
| BUG-111 | P1 | The cashier retained its desktop two-column workspace below tablet width, reducing the table map to a narrow strip and pushing actions/content out of practical reach. | Stack the table map and invoice workspace below 900px, use a compact one-column table-card layout on phones and constrain modal/history overflow to the viewport. | `cashierResponsive.test.js`, authenticated CDP desktop/tablet/mobile smoke. |
| BUG-112 | P1 | The employee table was wider than the admin mobile content area, clipping employee values and row actions even though the document itself reported no horizontal overflow. | Render employee rows as labelled responsive cards below 600px and keep both actions in a bounded two-column action grid. | `adminStaffResponsive.test.js`, authenticated CDP desktop/tablet/mobile smoke. |
| BUG-113 | P0 | A blank official database stopped short of the live JPA contract: demo hashes, staff/reservation columns, operational tables/views and table-area assignments were missing. | Add V060-V064 to repair only the canonical demo rows and create the missing compatible schema objects non-destructively. | `BlankDatabaseMigrationIntegrationTest`, `UniqueConstraintMigrationPreflightIntegrationTest`, six-role disposable runtime login/API smoke. |
| BUG-114 | P0 | Legacy order-detail price, table reserved-time, served order status and stale batch constraints rejected valid JPA writes. | Add V065-V068 to align defaults/types/statuses and remove only obsolete batch constraints. | Migration integration tests plus complete dine-in kitchen/payment/release E2E. |
| BUG-115 | P0 | Starting the first kitchen dish left its parent order PENDING, and manual cashier payment completed an order without consuming its inventory hold. | Transition the parent to IN_PREPARATION on kitchen start and consume the hold before manual payment completion. | `KitchenOrderDetailServiceTest`, `OrderWorkflowGuardTest`, dine-in E2E. |
| BUG-116 | P0 | An auto-assigned PENDING reservation could not move to deposit/payment milestones; confirming an already paid deposit attempted to move it backward to DEPOSIT_REQUIRED. | Permit payment milestones from PENDING and make confirmation payment-aware while preserving PAID deposits. | `ReservationStateMachineTest`, `ReservationServiceTest`, payment/deposit/confirm/check-in E2E. |
| BUG-117 | P1 | The scheduled expiry scan self-invoked a transactional item method, leaving lazy reservation data detached during realtime response construction. | Keep the bounded expiry batch inside an explicit transaction. | `ReservationServiceTest` plus accelerated disposable runtime scheduler smoke without lazy-load failures. |
| BUG-118 | P1 | Receipt status was updated from an after-commit callback without a new transaction, returning HTTP 500 after the payment itself had committed. | Run the receipt-status update in `REQUIRES_NEW`. | `ReservationReceiptServiceTest` plus a new paid reservation returning PAID with receipt status NOT_CONFIGURED. |
| BUG-119 | P0 | The baseline table-state constraint allowed only 0/1 although live workflows persist occupied, cleaning and merged states 2/3/5. | V069 replaces the constraint with the complete operational state set. | Blank/legacy migration tests plus check-in, merge and transfer E2E. |
| BUG-120 | P0 | Approving a reservation cancellation left its linked preorder kitchen order active, so refund completion could never release the table. | Cancel the unpaid linked preorder and release its inventory hold while deliberately retaining the table until the reservation refund completes. | `ReservationCancellationServiceTest`, `OrderRefundServiceTest`, cancel/approve/refund E2E. |
| BUG-121 | P1 | The canonical secondary token was navy and propagated blue into 356 theme references; multicolor emoji sidebar icons also ignored active/inactive CSS color. | Replace the secondary family with rose tokens, centralize Admin focus/interaction colors and render sidebar icons through one `currentColor` SVG component. | `themeTokens.test.js`, lint/build and 54-view packaged browser smoke. |
| BUG-122 | P1 | A zero-value reservation still entered the payment screen, per-dish notes made preorder unwieldy, bank details were incomplete, and legacy UI surfaces still rendered direct emoji icons. | Route zero-payable quotes directly to confirmation, reject zero-value QR creation before persistence, store one reservation-level kitchen note, expose configured bank metadata/copy controls, add representative area images, and remove direct emoji from frontend source while shared navigation uses `currentColor` SVG icons. | `reservationPaymentFlow.test.js`, `PaymentServiceTest`, `PaymentPropertiesTest`, migration integration tests, `themeTokens.test.js`, frontend lint/test/build and backend clean test/package. |
| BUG-123 | P1 | Pastel toasts had unreadable white text, action/stat icons could render as blank boxes, and legacy `alert()` calls opened blocking browser dialogs. | Standardize semantic toast colors, expand the shared `currentColor` `UiIcon`, replace blank/emoji surfaces and bridge legacy alerts to the global toast service. | `finalFixContracts.test.js`, `themeTokens.test.js`, frontend lint/test/build. |
| BUG-124 | P0 | Legacy `base_price` charged normal dining areas and mixed area identity with pricing, contradicting the free normal-area/VIP-fee policy. | Add the one-to-one `AreaPricing` model and V071, migrate private-room fees, force legacy base price to zero, and apply only active private-room fees during reservation pricing. | `TableAreaPricingContractTest`, `ReservationConcurrencyIntegrationTest`, blank/preflight migration tests, full backend suite. |
| BUG-125 | P1 | Table heatmap was disconnected from order data, floor-plan controls wrapped, and status chips overlapped. | Compute per-table use/revenue metrics from bounded order data, render a no-data state, and normalize non-wrapping controls plus wrapping status legends. | `finalFixContracts.test.js`, frontend lint/test/build. |
| BUG-126 | P0 | Waiters could not dispatch eligible orders to the kitchen, and overdue orders had no automated paid-order completion or admin warning. | Permit ADMIN/MANAGER/WAITER on the guarded dispatch transition, add a daily paid-only overdue completion service with system audit logs, and expose overdue dashboard metrics/warnings. | `EndpointAuthorizationMatrixTest`, `OverdueOrderServiceTest`, focused order tests and full backend suite. |
| BUG-127 | P1 | Product/category/knowledge/post administration had cramped tables, unbounded lists/content and misaligned actions. | Apply the 30/70 product layout, 20-item two-column category paging, compact 140-character knowledge previews with full modal, and horizontal post actions. | `finalFixContracts.test.js`, frontend lint/test/build. |
| BUG-128 | P1 | Inventory alerts omitted expired stock and AI purchase suggestions lacked sales/recipe/consumption/expiry context. | Separate expired/upcoming batch counts, show expired/expiring/low-stock alerts, and require evidence-based analysis plus a recommendation without fabricated data. | Ingredient repository/controller tests through the full backend suite and `finalFixContracts.test.js`. |
| BUG-129 | P1 | Customer invoice history could crash on nullable legacy order details/products and did not summarize CRM value. | Fetch the authenticated customer's detailed order graph transactionally, make frontend mapping null-safe and show total spend, usage count and loyalty points. | `finalFixContracts.test.js`, `AdminDataVisibilityRegressionTest`, full frontend/backend suites. |
| BUG-130 | P1 | Inventory dashboard counted expired batches while AI forecast and purchase suggestions only inspected aggregate/minimum stock, allowing 24 expired batches to be reported as “safe”; ingredient row actions also retained long text buttons. | Introduce one batch-aware `InventoryAlertService` for dashboard, AI and purchase suggestions; exclude expired batches from usable stock, combine completed sales with recipes for daily consumption, suppress purchases during expiry risk, and replace row actions with equal icon-only themed buttons/tooltips. | `InventoryAlertServiceTest`, `ChatbotControllerSecurityTest`, `OperationalOrderQueryScopeTest`, `finalFixContracts.test.js`, full frontend/backend suites. |
| BUG-131 | P0 | The active payment-intent unique index used nullable `reservation_id`, so SQL Server treated unrelated order QR intents as the same `(NULL, FULL)` key and the second valid customer checkout returned a generic 409. | V072 keys active intents by aggregate type/id/payment option; checkout now uses a transaction-owned idempotency key and returns the existing order/payment for a safe retry. | `OrderCheckoutServiceTest`, `OrderPaymentServiceTest`, `OrderCheckoutPersistenceIntegrationTest`, blank/legacy migration tests. |
| BUG-132 | P1 | Customer registration visually described acceptance but could be submitted without a server-enforced terms decision. | Add an accessible branded checkbox, disable/guard submit and require `termsAccepted=true` through Jakarta validation. | `AuthRequestValidationTest`, `registrationValidation.test.js`, `latestCustomerFlowContracts.test.js`. |
| BUG-133 | P1 | Public dine-in checkout rejected every request without a QR table capability even when the selected table was real, active and available. | Make QR optional while retaining locked server-side table/menu/quantity/inventory checks, transition an available table to occupied and notify Kitchen after checkout. | `OrderWorkflowGuardTest`, `OrderCheckoutServiceTest`. |
| BUG-134 | P1 | Dine-in suggestions asked only party size and could depend on fabricated AI JSON or include unavailable/allergy-conflicting items. | Collect favorites, palate and allergies; rank only persisted active products with positive recipe-backed servings and cap results at four. | `MenuRecommendationServiceTest`, `latestCustomerFlowContracts.test.js`. |
| BUG-135 | P1 | The customer menu remained four columns with an unpaged filtered list and left remaining-serving text visually unbalanced. | Use a resized six-column desktop grid, responsive 3–4/2-column layouts, centered stock messaging and filter-first pagination at 24 products without mutating cart state. | `menuPagination.test.js`, `latestCustomerFlowContracts.test.js`. |
| BUG-136 | P1 | The demo menu lacked the requested alcoholic beverage breadth and structured bottle metadata. | V073 adds 40 uniquely imaged products across eight beer/wine/spirit/sake categories, volume/ABV fields and recipe-backed stock. | `AlcoholMenuSeedContractTest`, `BlankDatabaseMigrationIntegrationTest`. |
| BUG-137 | P0 | Kitchen expiry rows dereferenced a nested `ingredient` object that the batch DTO never returns; auxiliary API and WebSocket failures also had no isolated section state, allowing runtime errors to blank operational content. | Resolve batch ingredient metadata by `ingredientId`, normalize API collections/numbers, isolate queue/inventory/menu errors and keep polling when realtime is unavailable; Kitchen inventory writes are narrowed to ADMIN/MANAGER. | `kitchenRender.test.js`, `kitchenData.test.js`, `router/index.test.js`, `EndpointAuthorizationMatrixTest`. |
| BUG-138 | P0 | Admin dispatch reused a payment-confirmation guard that rejected a valid paid PENDING order, while the frontend replaced every backend reason with a misleading permission message. | Add an ADMIN/MANAGER/WAITER-only dispatch endpoint, permit fully paid transfers, retain locked transition/inventory consumption, reject repeated/invalid/legacy dispatches specifically and surface the backend message. | `OrderPaymentServiceTest`, `EndpointAuthorizationMatrixTest`, `kitchenReservationFlowContracts.test.js`. |
| BUG-139 | P1 | Reservation Step 7 awaited quote calculation without error handling, so any API rejection left the step unchanged and silent; optional checkbox values were also serialized as an unbounded translated string. | Catch and display quote failures, guard duplicate navigation, use the quote's payable amount for 7→8/9, and serialize seven optional requirements into bounded explicit boolean flags while preserving the 500-character note. | `reservationSpecialRequirements.test.js`, `reservationPaymentFlow.test.js`, `kitchenReservationFlowContracts.test.js`. |
| BUG-140 | P0 | Expired batches remained in FEFO/available-stock queries, while near-expiry alerts suppressed replacement purchases and expired rows could be deleted without a disposal record. | Exclude expired batches from usable stock and FEFO, forecast replacement stock, add explicit batch lifecycle/status plus locked disposal audit, and block deletion of non-empty/expired batches. | `InventoryAlertServiceTest`, `IngredientBatchLifecycleServiceTest`, `MenuAvailabilityServiceTest`, blank-database migration. |
| BUG-141 | P1 | Menu activation did not consistently require a recipe, positive contribution margin and recipe-backed servings. | Centralize recipe cost/serving assessment, require price greater than cost and positive valid servings before activation, and expose cost/profit/margin/servings to Kitchen. | `MenuEconomicsServiceTest`, `AdminProductControllerTest`, `MenuAvailabilityServiceTest`. |
| BUG-142 | P0 | Waiter and Cashier used different table-order filters and payment/table cleanup were separate client mutations, so legacy table orders disappeared and paid state could diverge. | Share table/order predicates and floor→area grouping, match by canonical table ID, reject repeat payment, and move table to cleaning in the payment transaction. | `tableOperations.test.js`, `OrderWorkflowGuardTest`, `TableLifecycleServiceTest`, payment-focused suite. |
| BUG-143 | P1 | Staff schedule and attendance APIs returned `employee` while AdminStaff dereferenced `account`; LocalTime was parsed as a Date and payroll used hardcoded browser rates. | Align the UI contract, add loading/error/empty states and schedule edit/conflict validation, enrich attendance metrics, persist shift rates and calculate payroll server-side. | `staffVoucherFlowContracts.test.js`, `WorkScheduleConflictServiceTest`, `PayrollServiceTest`, `AttendancePolicyServiceTest`. |
| BUG-144 | P0 | Voucher validity was represented by one `isUsed` flag, with no pause, time window, multi-use limit or safe last-redemption concurrency. | Add lifecycle status, optional time/usage limits, admin toggle/edit/reset operations and pessimistically locked redemption shared by checkout/reservation. | `VoucherLifecycleServiceTest`, `VoucherConcurrencyIntegrationTest`, `staffVoucherFlowContracts.test.js`, blank-database migration. |
| BUG-145 | P0 | Recipe-backed menu cost was calculated dynamically, but the legacy `products.cost_price` values and initial selling prices were stale; two active dishes consequently sold below their real recipe cost. | Use the recipe and current ingredient price as the canonical cost, expose a shared configurable target margin/recommended price, reject activation at or below cost, and use V078 to synchronize legacy cost plus audit and pause every currently negative-margin dish without silently changing its price. | `MenuEconomicsServiceTest`, `AdminProductControllerTest`, `BlankDatabaseMigrationIntegrationTest`, full backend suite. |
| BUG-146 | P1 | Waiter and Cashier rendered table identity independently, so the business code/name was not guaranteed to remain prominent and consistent across cards and detail views. | Add one shared `tableIdentifier` selector (`code` then `name`, never database ID), use it on both cards and both detail headings, and protect it with responsive non-clipping styles. | `tableOperations.test.js`, `tableIdentifierContracts.test.js`, full frontend suite. |
| BUG-147 | P1 | Customer translations were split between JSON and JavaScript dictionaries while many customer views still embedded Vietnamese strings, so switching to English produced mixed-language flows. | Consolidate all locale content into parity-checked `vi.json`/`en.json`, localize Home/Menu/Booking/Checkout/Account flows and select bilingual API fields without falling back to Vietnamese in English mode. | `locales.test.js`, `restaurantTableLanguageUiContracts.test.js`, customer flow contract tests, full frontend suite. |
| BUG-148 | P1 | The original twenty seeded tables could not meet the declared area capacity and there was no idempotent expansion migration, leaving clients and recommendation/report consumers with an undersized shared dataset. | V079 preserves table IDs/order links, normalizes business codes, adds twenty tables and sets exact totals of Indoor 100, VIP 50 and Garden 70 seats through the shared table/area APIs. | `BlankDatabaseMigrationIntegrationTest`, public table/area DTO contracts, live SQL aggregate verification. |
| BUG-149 | P1 | Admin table-map toggle styles combined inline foreground colors with active red backgrounds, allowing red-on-red text/icons and inconsistent interaction states. | Remove conflicting inline colors and define shared primary/on-primary active, hover and pressed states for map and per-table actions. | `restaurantTableLanguageUiContracts.test.js`, frontend lint/build. |
| BUG-150 | P2 | Capacity values and table collections were rendered inconsistently: some omitted the `người` unit and Admin grouped only by floor while Waiter/Cashier used separate grouping paths. | Format capacity labels/forms with an explicit person unit and use the shared floor→area→tables grouping helper across Admin, Waiter and Cashier. | `tableOperations.test.js`, `restaurantTableLanguageUiContracts.test.js`, full frontend suite. |
| BUG-151 | P0 | Inventory imports could double-count aggregate stock and lacked a required invoice history record. | Route invoice, direct-batch and purchase approval imports through transactional `InventoryImportService`; add invoice code/detail persistence and V080 migration. | Blank migration, focused inventory/order tests. |
| BUG-152 | P1 | Expired/expiring inventory and menu availability could become inconsistent after scheduler updates. | Add lower-bound expiring query, recalculate ingredient aggregates and refresh menu after expiry; treat active no-recipe products as inventory-unmanaged. | `IngredientBatchLifecycleServiceTest`, `MenuAvailabilityServiceTest`. |
| BUG-153 | P1 | Admin order UI hardcoded VAT/totals, truncated order-code search and omitted statuses/realtime refresh. | Use backend financial fields, full-code search, map statuses 0–7 and subscribe to kitchen/orders/waiter topics. | Frontend test/lint/build. |
| BUG-154 | P1 | Admin had no dedicated expired-food processing screen and no aggregate disposal listing. | Add expired/disposed APIs, `/admin/expired-food` screen and reason-required disposal UX while preserving customer review route. | Frontend build and backend compile. |
| BUG-155 | P0 | Expired-food screen used prompt and could issue one request per batch; a refresh error could show failure after successful disposal. | Add transactional bulk disposal endpoint, themed modal, resilient `Promise.allSettled` refresh, green processed history/KPI updates. | Frontend 105 tests/lint/build; backend compile. |
| BUG-156 | P1 | Dine-in recommendation ignored current cart and heavy dishes defaulted to beer-first pairing. | Send real cart product IDs and rank non-alcoholic drinks first unless beer preference/cart signal exists; retain DB availability filter and fallback. | `MenuRecommendationService` compile and frontend suite. |
| BUG-157 | P1 | Reservation preorder quantity was not manually editable. | Add integer input with min/max validation, clamping and immediate quote invalidation. | Frontend suite/build. |
| BUG-158 | P0 | GitHub Actions ran SQL Server-backed Spring integration tests without a SQL Server instance, causing shared ApplicationContext failures. | Provision SQL Server 2022 in the CI job, wait for readiness, create `RestaurantDB`, and pass explicit database connection variables to Maven. | Full backend Maven suite in CI. |
| BUG-159 | P0 | Legacy Flyway migrations left SQL Server sessions with `NOCOUNT ON`, so the JDBC driver returned `-1` for Hibernate INSERT affected-row checks on a clean database. | Add V081 to reset the migration session and configure Hikari `connection-init-sql=SET NOCOUNT OFF` for every pooled datasource connection. | `BlankDatabaseMigrationIntegrationTest` verifies V001-V081 and NOCOUNT OFF; Attendance concurrency integration test verifies the application datasource state. |

## Master Prompt V2 flow audit (2026-08-20)

The detailed source-state, impact, flow, theme, generated-static, and verification report is in
`MASTER_PROMPT_V2_FLOW_AUDIT_2026-08-20.md`.

| Range | Result | Regression evidence |
|---|---|---|
| BUG-A01 | FIXED | `OrderRefundServiceTest` covers unpaid, partial/full payment and duplicate/concurrent-safe active refund handling. |
| BUG-A02-A04 | FIXED | `session.test.js` and `api.test.js` cover window-scoped identities, authorization selection, logout, 401 and password-change context. |
| BUG-A05-A10 | FIXED | Checkout, business-date/hours and reservation/event idempotency unit/integration regressions. |
| BUG-A11-A12 | FIXED | Persisted hashed table capabilities, rotation/revocation and add-more authorization in `TableSessionServiceTest`. |
| BUG-A13-A20 | FIXED | Structured order/table/area/schedule fields, query-backed activation, explicit KPI states and route cleanup regression. |
| BUG-A21-A25 | FIXED | Body-only waitlist lookup, shared hours, random codes, strict Java-time parsing and DTO validation regressions. |
| UI-BUG-01-06 | FIXED | Expanded source/generated-bundle palette scan, rebuilt Vite output and route-resource cleanup tests. Edge rendered 8 public routes with no legacy palette nodes or failed HTTP responses; exhaustive staff/admin, interaction-state and responsive review remains outstanding. |
