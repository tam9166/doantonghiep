# Regression tests

| ID | Scope | Assertion | Command/status |
|---|---|---|---|
| REG-SEC-001 | External API | No Authorization or X-Captcha-Token on `externalApi`. | `npm test` (existing `api.test.js`) |
| REG-REVIEW-020 | Review rating | Null or value outside 1..5 returns 422. | Add MVC test before changing review flow. |
| REG-SPA-012 | Change password | Direct route and refresh serve the SPA. | Browser/E2E required. |
| REG-BUILD-051 | Backend compilation | Source compiles after integrating fixes. | `mvnw.cmd -DskipTests compile` passed. |
| REG-ORDER-005 | Order atomicity | A late failure leaves no details, totals, inventory mutations or idempotency record. | `OrderCheckoutPersistenceIntegrationTest` |
| REG-STOCK-006 | Inventory shortage | Insufficient stock creates nothing and preserves order totals plus batch/ingredient quantities. | `OrderCheckoutPersistenceIntegrationTest`; maps to HTTP 409 via `GlobalExceptionHandlerTest`. |
| REG-STOCK-007 | Inventory race | Two database transactions requesting the last stock yield exactly one success and stock never becomes negative. | `OrderCheckoutPersistenceIntegrationTest` |
| REG-ORDER-008 | Add-items retry | Reusing the same idempotency key and payload returns the stored result without a second detail or inventory deduction. | `OrderCheckoutPersistenceIntegrationTest` |
| REG-MONEY-009 | Exact numeric types | Inventory, recipe and tax fields use `BigDecimal`/`DECIMAL`; `0.1 × 3` consumes exactly `0.3000` and all six migrated columns are audited at runtime. | V045 and `OrderCheckoutPersistenceIntegrationTest` |
| REG-AI-052 | Availability | Only active tables with sufficient capacity are returned. | `AiAvailabilityToolServiceTest` |
| REG-TIME-053 | Last order | Exact cutoff and later times reject new orders. | `RestaurantBusinessHoursServiceTest` |
| REG-AI-054 | Conversation date | “Tomorrow” uses the injected clock in Vietnam timezone. | `AiDynamicToolServiceTest` |
| REG-AI-055 | Intent | “Sắp hết” means low stock; “sắp hết hạn” means expiry. | `RoleAwareAssistantServiceTest` |
| REG-BOOKING-056 | Legacy booking | Deprecated guest booking returns 410 and does not write PII into order address. | `OrderWorkflowGuardTest` |
| REG-RES-057 | Reservation state | Pending reservation must pass table assignment before payment/confirmation. | `ReservationStateMachineTest`, `ReservationServiceTest` |
| REG-AI-058 | Conversation memory | Messages without a guest count retain a nullable fallback instead of throwing during ternary unboxing. | `AiConversationMemoryServiceTest`; passed in full backend suite. |
| REG-AUTH-059 | Staff login | Authentication loads account authorities and their roles before the repository session closes. | `AccountTokenSecurityTest` plus live staff-login smoke test. |
| REG-ADMIN-060 | Admin data | Staff, popular items and reservations load their required relationships without LAZY/entity-graph failures. | `AdminDataVisibilityRegressionTest` plus authenticated API smoke checks. |
| REG-AUTH-061 | Self-service IDOR | Timekeeping, personal schedule and personal service-zone APIs always use the authenticated principal even if another username is supplied. | `SelfServiceDataIsolationIntegrationTest` |
| REG-SEC-062 | Account response | Direct Account serialization and normal staff/customer/order admin responses contain no password/hash/token-version/internal authority fields. | `SelfServiceDataIsolationIntegrationTest` |
| REG-SEC-063 | JWT startup | Production and staging fail fast when the JWT signing secret is absent; development retains its explicit local-only fallback. | `ApplicationStartupValidatorTest` |
| REG-SEC-064 | Production credentials | Every production/staging account must have a BCrypt hash and cannot use common demo credentials. | `ProductionCredentialValidatorTest` plus tracked configuration audit. |
| REG-ORDER-065 | Explicit order type | Checkout rejects a populated request without `orderType`; delivery/takeaway/dine-in callers must state their flow. | `OrderCheckoutServiceTest` and `OrderCheckoutPersistenceIntegrationTest` |
| REG-RATE-066 | Profile fail-closed | Base/prod/production enable rate limiting; only dev/test/local example disable it by default. | `RateLimitProfileConfigurationTest` |
| REG-RES-067 | Bounded expiry scan | Expiry/no-show processing receives only repository-selected candidates and never reuses the admin full-table listing. | `ReservationServiceTest` plus the candidate query and V050 indexes |
| REG-SESSION-068 | Customer/staff isolation | Staff requests never fall back to customer tokens; each logout preserves the other active namespace. | `api.test.js`, `session.test.js` |
| REG-MIGRATION-069 | Unique preflight | Blank and valid legacy schemas migrate; duplicate review IDs or reservation codes fail before unique-index migrations with actionable messages. | `BlankDatabaseMigrationIntegrationTest`, `UniqueConstraintMigrationPreflightIntegrationTest` |
| REG-UI-070 | Canonical theme | Legacy green/gold/cream palette values cannot reappear in Vue/CSS source. | `themeTokens.test.js` |
| REG-REFUND-071 | Cancel/refund ledger | Refundable value is derived from successful payment/refund operations, never `deposit / 2`; a repeated request reuses the active refund. | `OrderRefundServiceTest` |
| REG-SESSION-072 | Window-scoped auth | Customer, waiter, kitchen, cashier and admin windows retain isolated token/user/context values through logout, 401 and password change. | `session.test.js`, `api.test.js` |
| REG-ORDER-073 | Delivery contract | Customer delivery checkout always sends `orderType=DELIVERY`. | `orderCheckout.test.js` |
| REG-RES-074 | Shared quote/create assignment | Quote accepts auto-table inputs and uses the same assignment path as create; payment selection is independent of preorder. | `ReservationServiceTest`, frontend reservation assertions |
| REG-TIME-075 | Vietnam business date/hours | Frontend local dates use Asia/Ho_Chi_Minh and public hours support normal and overnight windows. | `businessDate.test.js`, `businessHours.test.js`, `RestaurantBusinessHoursServiceTest` |
| REG-EVENT-076 | Booking retry | A repeated event-booking idempotency key returns the original booking and a changed payload conflicts. | `ReservationServiceTest` |
| REG-QR-077 | Table capability | Only the current random capability can resolve/add items; rotation, close and cancellation revoke the previous capability. | `TableSessionServiceTest` |
| REG-ORDER-078 | Structured order contract | Operational screens receive order type, table/area, delivery address, order code and scheduled timestamp without parsing address text. | `OrderResponsePrivacyTest`, `OrderWorkflowGuardTest` |
| REG-WAITLIST-079 | Private/race-safe lookup | Lookup phone is in a validated POST body, business hours are shared, and codes are random with collision retry. | `ReservationWaitlistServiceTest`, `RateLimitingFilterTest` |
| REG-SCHEDULE-080 | Strict schedule input | Invalid/missing username, date or shift is rejected by Bean Validation and invalid dates are not leniently parsed. | `WorkScheduleRequestValidationTest` |
| REG-UI-081 | Route resources/theme output | Persistent route timers are paired with unmount cleanup and legacy HEX/RGB/RGBA is rejected in source and backend static output. | `resourceCleanup.test.js`, `themeTokens.test.js` |
| REG-SPA-082 | SPA refresh registry | Event booking, password change and every admin/customer/staff Vue route share one exact MVC/security route registry; index cannot forward to itself. | `SpaRouteRegistryTest` plus 34-route runtime HTTP smoke |
| REG-RES-083 | SQL Server expiry query | The no-show query compares `arrival_time TIME` with an explicitly compatible time value and remains capped at 200 candidates. | `ReservationExpiryQueryIntegrationTest` plus accelerated runtime scheduler smoke |
| REG-MENU-084 | Hot-menu schema contract | The public hot-menu query uses the canonical `order_details` table and executes against SQL Server. | `HotMenuItemQueryIntegrationTest` plus packaged-browser HTTP smoke |
| REG-QR-085 | Public dine-in suggestions | The guest dine-in screen loads frequently ordered dishes from a public endpoint and never depends on an admin API. | `publicEndpointContracts.test.js` plus packaged-browser HTTP smoke |
| REG-CANCEL-086 | Same-booking cancellation verification | At least two of code/name/phone/email must match one reservation; split matches, closed bookings and duplicate requests fail safely. | `ReservationCancellationServiceTest` |
| REG-REFUND-087 | Exact cancellation boundary | 13h, 12h01 and exactly 12h refund 50% of actual paid deposit; 11h59 and past bookings refund zero. | `ReservationCancellationPolicyTest` |
| REG-REFUND-088 | Honest refund completion | A requested refund remains pending; completion writes a successful REFUND ledger entry with an external reference. | `RefundServiceTest` |
| REG-PII-089 | Public cancellation receipt | Public creation returns only request code/status/message, and reservation lookup never persists phone in a URL. | `publicEndpointContracts.test.js`, cancellation service return contract |
| REG-CONTACT-090 | Append-only call history | Repeated reservation contact updates create history rows instead of overwriting evidence. | `ReservationServiceTest` |
| REG-STOCK-091 | Recipe serving limit | Available servings use the minimum floor across recipe ingredients from batch stock; requests above it are rejected before order creation. | `MenuAvailabilityServiceTest`, `OrderCheckoutServiceTest` |
| REG-UI-092 | Customer quantity cap | Delivery and dine-in carts use `availableQuantity`, show sold-out state and stop increments beyond stock. | `publicEndpointContracts.test.js` |
| REG-RATE-093 | Anonymous checkout abuse | Public checkout has a dedicated 10 requests/minute/IP policy before the broad API limit. | `RateLimitingFilterTest` |
| REG-STOCK-094 | Checkout hold | Creating an order persists a recipe-derived hold without mutating physical FIFO batches. | `InventoryReservationServiceTest`, `OrderCheckoutServiceTest` |
| REG-STOCK-095 | Consume once | Full ledger payment or authorized COD/tại-quán dispatch consumes held FIFO stock exactly once. | `InventoryReservationServiceTest`, `OrderPaymentServiceTest` |
| REG-STOCK-096 | Release | Order cancellation and expired transfer intents release active holds without increasing/decreasing physical batches. | `InventoryReservationServiceTest`, `OrderRefundServiceTest`, `PaymentLedgerServiceTest` |
| REG-STOCK-097 | Public availability | Numeric servings subtract only unexpired `RESERVED` quantities from physical batch stock. | `MenuAvailabilityServiceTest` |
| REG-MIGRATION-098 | Inventory hold schema | V055 applies from both blank and V045 legacy fixtures and creates `inventory_reservations` with compatible keys/indexes. | `BlankDatabaseMigrationIntegrationTest`, `UniqueConstraintMigrationPreflightIntegrationTest` |
| REG-KITCHEN-099 | Dish cancellation money/audit | A canceled dish records actor/reason/time, removes its discounted line/tax from the parent order and creates only a `PENDING` refund for paid excess. | `KitchenOrderDetailServiceTest` |
| REG-STOCK-100 | Dish cancellation inventory | A reserved dish reduces its hold; an unstarted consumed dish compensates stock once; a started dish is treated as physical waste and is not restocked. | `InventoryReservationServiceTest`, `KitchenOrderDetailServiceTest` |
| REG-MIGRATION-101 | Kitchen cancellation audit | V056 adds `cancelled_by` and the order/status index without breaking a blank or V045 legacy migration path. | `BlankDatabaseMigrationIntegrationTest`, `UniqueConstraintMigrationPreflightIntegrationTest` |
| REG-ORDER-102 | Canonical order transitions | Unknown values, skipped `PENDING -> SERVED` changes and rollback from terminal cancellation are rejected by one state machine. | `OrderStateMachineServiceTest` |
| REG-KITCHEN-103 | Parent ready derivation | The parent becomes partially ready after the first dish and ready only after every active dish is complete. | `OrderStateMachineServiceTest`, `KitchenOrderDetailServiceTest` |
| REG-WAITER-104 | Least-privilege serving | WAITER can request only detail status 2, only for a completed dish; all active dishes must be served before the parent reaches SERVED. | `EndpointAuthorizationMatrixTest`, `KitchenOrderDetailServiceTest`, `OrderWorkflowGuardTest` |
| REG-TABLE-105 | Financially safe merge/split | Any paid amount, payment intent/ledger, refund, voucher or active inventory hold blocks composition until allocation is explicitly supported. | `OrderFinancialMutationGuardServiceTest` |
| REG-TABLE-106 | Exact split selection | Duplicate, missing or foreign detail IDs fail the whole transaction instead of being silently skipped; target must be active and have no open order. | `OrderWorkflowGuardTest` |
| REG-TABLE-107 | Safe transfer | Disabled/occupied/conflicting targets are rejected and a successful move revokes both QR sessions while releasing only an unused source. | `OrderWorkflowGuardTest` |
| REG-TABLE-108 | Shared release guard | Unpaid balance, pending payment/refund or unserved dishes block status/unlink; fully paid and served orders complete before release. | `TableReleaseGuardServiceTest`, `OrderWorkflowGuardTest` |
| REG-EVENT-109 | Event deposit capability | Event creation persists deposit expiry/owner, returns a payment capability and emits manager notification plus realtime event. | `ReservationServiceTest` |
| REG-EVENT-110 | Event QR path | The event UI retains the capability and immediately requests a deposit QR without discarding a successfully created booking if QR generation fails. | Frontend build plus source contract |
| REG-RATE-111 | Event public abuse | Public event creation requires the `event-booking-create` CAPTCHA action and is limited to 10 requests/minute/IP. | `CaptchaFilterTest`, `RateLimitingFilterTest`, `captcha.test.js` |
| REG-AUTH-112 | Registration contract parity | Email is required and structurally validated on both sides; username/full-name limits, password length/common-password rejection and identity normalization run before confirmation and again before submit. | `AuthRequestValidationTest`, `PasswordPolicyTest`, `registrationValidation.test.js` |
| REG-AUTH-113 | Dedicated staff profile context | Kitchen/Waiter/Cashier/Admin profile navigation uses `/staff/profile`; refresh is SPA-safe, the route always selects the staff token, `/staff-login` is never mistaken for a protected staff workspace, and logout clears `auth_context`. | `index.test.js`, `session.test.js`, `api.test.js`, `SpaRouteRegistryTest` |
| REG-WAITLIST-114 | Reachable no-table fallback | Reservation UI offers the public waitlist action only when neither a suitable single table nor a backend-proposed combination exists; the obsolete manual table picker cannot silently return. | `publicEndpointContracts.test.js`, `ReservationWaitlistServiceTest`, `RateLimitingFilterTest` |
| REG-TABLE-115 | Canonical time-aware availability | Both public table-availability routes apply date, time, duration, capacity, area and late-dining policy; a booking beginning on the previous date still blocks an overlapping after-midnight request. | `RestaurantTableAvailabilityControllerTest`, `ReservationServiceTest`, `RestaurantBusinessHoursServiceTest` |
| REG-ORDER-116 | Structured delivery identity | DELIVERY rejects missing/invalid recipient data and the browser sends name, phone, address and note as separate fields. | `OrderCheckoutServiceTest`, `orderCheckout.test.js` |
| REG-VOUCHER-117 | Exact voucher attribution | A 10% membership reduction followed by a 20% voucher on 100,000 records 10,000 membership and exactly 18,000 voucher discount, never a combined rate on final total. | `OrderCheckoutServiceTest` |
| REG-SCHEDULE-118 | Complete shift snapshot | Every new schedule writes shift name, start/end times and status required by SQL Server. | `WorkShiftDefinitionTest`, `SelfServiceDataIsolationIntegrationTest` |
| REG-MIGRATION-119 | Delivery/discount/schedule schema | V057 adds and backfills structured delivery, exact discount and required schedule columns on blank and V045 legacy databases. | `BlankDatabaseMigrationIntegrationTest`, `UniqueConstraintMigrationPreflightIntegrationTest` |
| REG-ATTENDANCE-120 | One attendance row and real shifts | Check-in locks the employee, lateness follows the assigned schedule/grace instead of fixed clocks, and overnight check-out calculates one decimal-hour value. | `AttendancePolicyServiceTest`, `SelfServiceDataIsolationIntegrationTest` |
| REG-MIGRATION-121 | Attendance integrity schema | V058 backfills `total_hours`, fails actionably on duplicate employee/date rows and creates the unique attendance index on blank and V045 legacy paths. | `BlankDatabaseMigrationIntegrationTest`, `UniqueConstraintMigrationPreflightIntegrationTest` |
| REG-KITCHEN-122 | Bounded realtime board | Kitchen loads the scoped board endpoint, never the full admin order list, uses WebSocket primarily and polls no faster than 60 seconds. | `OperationalOrderQueryScopeTest`, `kitchenPerformance.test.js` |
| REG-QUERY-123 | Operational query scope | Admin order arrays are capped; dashboard/revenue/waiter/kitchen/reservation/popular/purchase/assistant flows delegate status/date/active filtering to repositories rather than loading all historical rows. | `OperationalOrderQueryScopeTest`, `AdminDataVisibilityRegressionTest`, `ReservationServiceTest`, `RoleAwareAssistantServiceTest` |
| REG-REPORT-124 | Decimal ingredient ranking | A non-empty popular-ingredient report sorts `BigDecimal` consumption without primitive-cast failure. | `AdminDataVisibilityRegressionTest` |
| REG-UI-125 | Shared dangerous-action dialog | Confirm and required-reason prompt share one global state and resolve explicit user decisions for payment/refund/table/kitchen flows. | `dialogAndPrint.test.js` |
| REG-PRINT-126 | Vue-safe invoice printing | Printing clones the selected rendered element and never replaces the live application body or reloads the route. | `dialogAndPrint.test.js` |
| REG-APPLICATION-127 | Recruitment metadata | Multipart name, phone, email, message and post ID are validated before file/persistence work; CV content remains size/type/structure validated. | `ApplicationRequestValidationTest`, `CvFileStorageServiceTest` |
| REG-ERROR-128 | No silent persisted-data corruption | Invalid JSON stored in an entity column raises an explicit conversion failure instead of becoming an empty list. | `StringListJsonConverterTest` |
| REG-BUILD-129 | Clean dependency reproduction | Lockfile-only install, zero-vulnerability audit, lint, tests and production build succeed; Maven wrapper retains Linux execute permission. | `npm ci`, `npm audit`, `npm run lint`, `npm test`, `npm run build`, `mvnw test/package` |
| REG-TABLE-130 | Single table release writer | Every release locks tables, evaluates financial/dish/inventory invariants, changes availability and revokes QR capability in one transaction; pending refunds retain the table. | `TableLifecycleServiceTest`, `TableReleaseGuardServiceTest`, `OrderRefundServiceTest`, `ReservationCancellationServiceTest` |
| REG-IDOR-131 | Role-scoped notifications | Waiter/cashier list, unread count, single-read and read-all operations cannot address another role's notification. | `NotificationAuthorizationTest` |
| REG-WS-132 | Destination authorization | Anonymous/customer/staff and mismatched operational roles cannot subscribe/send; ADMIN, MANAGER, KITCHEN, WAITER and CASHIER receive only their allowed destinations. | `WebSocketConfigTest` |
| REG-REFUND-133 | Exact completed-refund retry | A repeated confirmation succeeds only for the same successful refund-ledger reference and rejects a changed reference. | `RefundServiceTest`, `OrderRefundServiceTest` |
| REG-CONCURRENCY-134 | Deterministic table workflow locks | Merge/split lock both tables and involved orders by ID; transfer locks source and target before the order and rejects a changed source. | `OrderWorkflowGuardTest` |
| REG-CONCURRENCY-135 | Voucher/waitlist serialization | Reservation creation locks a voucher before setting its one-time flag; waitlist terminal transitions lock the entry. | `ReservationVoucherLockingTest`, `ReservationWaitlistServiceTest` |
| REG-RESERVATION-136 | Overnight capacity and assignment | A 23:30 reservation continuing after midnight blocks both capacity and the physical table for an overlapping next-day request. | `AutoTableAssignmentServiceTest`, `RestaurantCapacityServiceTest` |
| REG-UI-137 | Waiter shared confirmation UI | Guest-departure, unlink, check-in, booking cancellation and shift logout actions use the responsive global dialog rather than native browser confirmation. | Frontend lint, 55-test suite and production build |
| REG-CONCURRENCY-138 | Reservation idempotency and last-table race | Two distinct requests competing for the final table produce exactly one reservation and one conflict; two concurrent retries with the same idempotency key both resolve successfully while persisting exactly one reservation. | `ReservationConcurrencyIntegrationTest`, `ReservationServiceTest` |
| REG-CONCURRENCY-139 | Exclusive waitlist conversion | Conversion requires a real matching reservation; two waitlist entries racing to claim it produce one converted entry and one conflict, with V059 preventing duplicate links at the database layer. | `ReservationConcurrencyIntegrationTest`, `ReservationWaitlistServiceTest`, migration integration tests |
| REG-UI-140 | Admin reservation shared prompts | Contact, confirm, reject, cancel and waitlist actions use the shared dialog; browser cancellation aborts the request and conversion cannot submit without a reservation code. | `adminReservationDialog.test.js`, frontend lint/test/build |
| REG-CONCURRENCY-141 | Single-use voucher race | Two reservations competing for the final voucher use produce one success and one 422; exactly one usage row is stored and the voucher is consumed once. | `ReservationConcurrencyIntegrationTest`, `ReservationVoucherLockingTest` |
| REG-CONCURRENCY-142 | Attendance double check-in | Two simultaneous check-ins for one employee/business date produce one success, one validation failure and exactly one timekeeping row. | `AttendanceConcurrencyIntegrationTest`, `AttendancePolicyServiceTest` |
| REG-CONCURRENCY-143 | Duplicate payment callback | Two simultaneous credits with the same provider transaction both resolve idempotently, create exactly one successful ledger row and apply the amount once. | `PaymentConcurrencyIntegrationTest`, `PaymentLedgerServiceTest`, `PaymentWebhookServiceTest` |
| REG-CONCURRENCY-144 | Contended table transfer | Two waiter requests moving different open orders into one target table produce exactly one success and leave exactly one active target order; the target QR capability is revoked once. | `TableWorkflowConcurrencyIntegrationTest`, `OrderWorkflowGuardTest` |
| REG-CONCURRENCY-145 | Contended table merge | Two merges competing for one source serialize on locked tables/orders, cancel the source exactly once and revoke its obsolete QR capability exactly once. | `TableWorkflowConcurrencyIntegrationTest`, `OrderWorkflowGuardTest` |
| REG-CONCURRENCY-146 | Idempotent concurrent table release | Two releases of one table serialize, leave one consistent AVAILABLE state and revoke the table capability once. | `TableReleaseConcurrencyIntegrationTest`, `TableLifecycleServiceTest` |
| REG-CONCURRENCY-147 | Payment callback versus release | Release waits on the callback-held payment-intent lock and evaluates the committed PAID order instead of a stale pre-callback snapshot. | `TableReleaseConcurrencyIntegrationTest`, `TableReleaseGuardServiceTest` |
| REG-UI-148 | Atomic waiter table move | Waiter submits exactly one transfer command; frontend cannot replay target/source status mutations after the backend transaction commits. | `resourceCleanup.test.js` |
| REG-CONFIG-149 | Production CAPTCHA fail-closed | Production profiles default CAPTCHA on and startup rejects disabled/mock/secretless CAPTCHA configuration. | `ApplicationStartupValidatorTest`, `CorsProfileConfigurationTest` |
| REG-UI-150 | Authenticated multi-role responsive smoke | All six demo roles authenticate through their correct login gate; 11 protected screens load their expected route at 1366x768, 768x1024 and 390x844 without JavaScript exceptions, failed requests or document-level horizontal overflow. | `scripts/cdp-role-responsive-smoke.mjs` against the packaged application |
| REG-UI-151 | Responsive cashier workspace | Cashier stacks the table map and invoice on tablet/mobile, preserves full-width actions and keeps every visible button within the horizontal viewport. | `cashierResponsive.test.js`, authenticated CDP captures |
| REG-UI-152 | Responsive employee cards | The employee list changes from a wide table to labelled mobile cards with bounded edit/delete actions. | `adminStaffResponsive.test.js`, authenticated CDP captures |
| REG-MIGRATION-153 | Complete blank runtime schema | V060-V069 produce the account, reservation, operational, order, inventory and table-state contract required by the packaged application. | `BlankDatabaseMigrationIntegrationTest`, `UniqueConstraintMigrationPreflightIntegrationTest` |
| REG-KITCHEN-154 | Parent start and cashier stock finalization | Starting a first dish moves the parent to IN_PREPARATION; manual payment consumes the active inventory hold before completion. | `KitchenOrderDetailServiceTest`, `OrderWorkflowGuardTest` |
| REG-RESERVATION-155 | Auto-assigned payment lifecycle | A PENDING auto-assigned booking can become deposit/full paid, manager confirmation never reverses PAID deposit state, and check-in succeeds. | `ReservationStateMachineTest`, `ReservationServiceTest`, disposable runtime E2E |
| REG-RESERVATION-156 | Transactional expiry batch | Expiry candidates stay attached while history, response, realtime and audit data are built. | `ReservationServiceTest`, accelerated runtime scheduler smoke |
| REG-PAYMENT-157 | Independent receipt status update | After-commit receipt status persistence always starts a new transaction and cannot turn a committed payment response into HTTP 500. | `ReservationReceiptServiceTest`, disposable runtime payment E2E |
| REG-TABLE-158 | Complete table-state constraint | SQL Server accepts only operational states 0, 1, 2, 3 and 5, including check-in, cleaning and merged-table writes. | V069 migration assertions, check-in/merge/transfer E2E |
| REG-CANCEL-159 | Linked preorder cancellation | Reservation cancellation releases the preorder inventory hold and cancels its unpaid kitchen order without releasing the table before refund completion. | `ReservationCancellationServiceTest`, `OrderRefundServiceTest`, disposable runtime refund E2E |
| REG-E2E-160 | Critical mutable business flows | Six-role login; reservation payment/check-in; order-kitchen-waiter-cashier-release; cancellation/refund; merge/split/transfer all persist and survive API refresh checks on a disposable SQL Server database. | Packaged runtime against `CodexE2E_Final_20260823` |

Latest completed full verification: backend clean package passed with 420 tests, 0 failures/errors/skips; frontend clean install/audit, lint, 61 tests and Vite production build passed. Blank and V045 legacy SQL Server migration paths pass through V069.
