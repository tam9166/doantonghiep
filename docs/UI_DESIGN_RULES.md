# UI design rules

Use semantic tokens for primary, success, warning, danger, info, surface, border and text. Every order/table/reservation/payment status must include text or icon in addition to color. Keep button meanings consistent across screens, ensure keyboard focus/contrast, and test desktop, tablet and mobile before release. Native `alert`, `confirm`, and `prompt` are technical debt; use the shared dialog/toast pattern for changed screens.
